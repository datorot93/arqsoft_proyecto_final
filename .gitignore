# Configuración local de Claude Code (por usuario, no se versiona)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/

# Sistema
.DS_Store
*~

# Java / Gradle (F4 — services/)
**/build/
**/.gradle/
*.class
*.jar
!services/gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
out/
hs_err_pid*

# Resultados de corridas (F6) — los timestamps por ronda son efímeros.
# El aggregate y un sample report se commitean explícitamente con git add -f.
runs/results/

# Lock interno de scheduled_tasks (no versionado)
.claude/scheduled_tasks.lock
