#!/bin/bash
# cloud-init para nodos worker de OKE — Línea Verde
# El script es gestionado automáticamente por OKE Enhanced Cluster.
# Se mantiene mínimo para no interferir con el bootstrap del operador.
# cluster_id: ${cluster_id}
echo "Node bootstrap for cluster ${cluster_id} — Linea Verde"
